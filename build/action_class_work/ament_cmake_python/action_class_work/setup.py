from setuptools import find_packages
from setuptools import setup

setup(
    name='action_class_work',
    version='0.0.0',
    packages=find_packages(
        include=('action_class_work', 'action_class_work.*')),
)
